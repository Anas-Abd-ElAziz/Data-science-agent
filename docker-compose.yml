services:
  api:
    build: .
    container_name: data-science-api
    ports:
      - "8000:8000"
    env_file:
      - .env
    command: ["uvicorn", "api:app", "--host", "0.0.0.0", "--port", "8000"]
    restart: unless-stopped
